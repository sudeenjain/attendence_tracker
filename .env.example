# Port for local development Express server
PORT=3000

# PostgreSQL Connection String (Optional, for persistent cloud deployments like Supabase/Render)
# If left blank, the app will automatically fallback to local SQLite database.
# Example: postgresql://postgres:password@localhost:5432/attendance
DATABASE_URL=
